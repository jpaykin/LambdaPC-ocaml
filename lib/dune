;(include_subdirs unqualified)

;(menhir
; (modules parser))

;(ocamllex
; (modules lexer))

(library
 (name pCLib)
 ;(modules :standard)
 (modules
   ;Interface
   ;Parser
   ;Lexer
 )
 (libraries
  ; internal
  interface core
  ; external
  smtml
 )
)
